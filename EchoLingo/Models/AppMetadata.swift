import Foundation

enum AppMetadata {
    static let appName = "EchoLingo"
    static let version = "1.0 MVP"
    static let buildGoal = "Real-time captions + live translation"
    static let privacyURL = URL(string: "https://example.com/privacy")!
    static let supportURL = URL(string: "https://example.com/support")!
    static let githubURL = URL(string: "https://github.com/")!
}
